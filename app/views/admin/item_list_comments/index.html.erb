投稿アイテムコメント一覧

<% if @item_list_comments.present? %>

<% @item_list_comments.each do |item_list_comment| %>

会員
<%= item_list_comment.user_id %>
<%= item_list_comment.user.nick_name %>
コメント

<%= item_list_comment.comment %>
<%= link_to "詳細を確認する", admin_item_list_path(item_list_comment.item_list_id) %>

<% end %>

<% else %>
    投稿しているコメントはありません
<% end %>
