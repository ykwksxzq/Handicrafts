アイテムコメント一覧

<% if @item_list_comment.present? %>
会員ID<%= @item_list_comment.user.id %>
ニックネーム<%= @item_list_comment.user.nick_name %>
コメント<%= @item_list_comment.comment %>

<%= link_to "コメントを削除する", admin_item_list_comment_path(@item_list_comment), class: "btn btn-danger btn-lg", method: :delete, "data-confirm" => "本当にコメントを削除しますか?" %>

<% else %>
  保存しているコメントはありません
<% end %>
