 <div class="form-group row">
  <div class="field">
   <h6 class="text-dark p-2 mb-0">アイテム名(30文字以内)</h6>
    <%= f.text_field :item_name, class: 'form-control', placeholder:"30文字以内で入力"%>
  </div>
 </div>

 <div class="form-group row">
  <div class="field">
   <h6 class="text-dark p-2 mb-0">メモ(60文字以内)</h6>
    <%= f.text_area :memo, class: 'form-control', placeholder:"60文字以内で入力" %>
  </div>
 </div>

 <div class="form-group row">
  <div class="field">
   <h6 class="text-dark p-2 mb-0">ジャンル
    <%= f.collection_select :genre_id, Genre.all, :id, :name, class: 'form-control'%></h6>
  </div>
  </div>
